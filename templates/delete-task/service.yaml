apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-delete-tasks
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-delete-tasks
  ports:
    - port: {{ .Values.deleteTask.ServicePort }}
      targetPort: {{ .Values.deleteTask.port }}
